@using Dealership.Models;

<h1>Car List</h1>

@if (Model.Count == 0)
{
    <p>There are no items in the list.</p>
}
<ul>
    @foreach (Car cars in Model)
    {
        <li>@cars.MakeModel</li>
    }
</ul>
<a href="/cars/new">Add a new car.</a>